/** Injected by vite.config.ts `define` — the commit the bundle was built from. */
declare const __BUILD_ID__: string;
/** Injected by vite.config.ts `define` — ISO timestamp of the build. */
declare const __BUILD_TIME__: string;
